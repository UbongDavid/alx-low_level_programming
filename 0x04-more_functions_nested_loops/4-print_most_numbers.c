#include "main.h"

/**
 * print_most_numbers - check if the entered character is lowercase alphabet
 * @c: Integer Variable
 * Return: Always 0.
 */

int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
