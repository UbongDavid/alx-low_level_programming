#include "main.h"
#include <ctype.h>
/**
 * print_sign - Thisfunction prints the sign of a number
 * @n: Integer Variable
 * Return: Varies
 */

int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+')
		return (1);
	}
elseif(n = 0)
	{
		_putchar('0')
		return (0);
	}
else
{
	_putchar('-')
		return (-1)
		}
}
